# Backend (Node) host:port — must match apps/server/.env PORT
VITE_BACKEND_HOST=localhost
VITE_BACKEND_PORT=3001

# Cesium Ion access token — get one at https://ion.cesium.com/tokens
# Optional: the globe falls back to offline imagery if empty.
VITE_CESIUM_ION_TOKEN=
